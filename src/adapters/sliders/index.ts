import type { InputAdapter } from '../input-adapter';

/** Manual slider adapter (dev builds only). Stub — implemented in M1. */
export function createSliderAdapter(): InputAdapter {
  throw new Error('slider adapter not implemented yet (M1)');
}
