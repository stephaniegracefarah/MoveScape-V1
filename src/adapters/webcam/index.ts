import type { InputAdapter } from '../input-adapter';

/** Webcam + pose adapter. Stub — implemented in M1. */
export function createWebcamAdapter(): InputAdapter {
  throw new Error('webcam adapter not implemented yet (M1)');
}
